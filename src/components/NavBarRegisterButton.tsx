import { JBMono } from "@/fonts"

export const NavBarRegisterButton = () => {
    return (
        <button className={`${JBMono.className} text-3xl bg-[#5bbab5] hover:bg-[#890095] hover-ease-in ease-out duration-100 cursor-pointer border-2 p-1 text-white border-black register_button`}>Register!</button>
    )
}