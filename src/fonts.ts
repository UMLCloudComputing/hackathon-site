import { Instrument_Sans, JetBrains_Mono } from "next/font/google"

export const JBMono = JetBrains_Mono({
    subsets: ["latin"]
})

export const InstrSans = Instrument_Sans({
    subsets: ["latin"]
})